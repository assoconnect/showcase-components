const shadowsSize = {
  big: '0 10px 50px 0',
}

const shadows = {
  header: `${shadowsSize.big} rgba(0,9,44,.15)`,
  slider: `${shadowsSize.big} rgba(0,9,44,.20)`,
  sliderLight: `${shadowsSize.big} rgba(0,9,44,.1)`,
}

export default shadows
